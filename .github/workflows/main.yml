name: Infrastructure Plan

on:
  push:
    branches:    
      - 'master'
    paths:
      - 'infrastructure/**'

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
